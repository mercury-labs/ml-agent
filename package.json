{
  "name": "slack-lists-cli",
  "version": "0.0.1",
  "description": "CLI for Slack Lists",
  "bin": {
    "slack-lists": "dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@slack/web-api": "^7.13.0",
    "commander": "^11.0.0",
    "playwright": "^1.55.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
